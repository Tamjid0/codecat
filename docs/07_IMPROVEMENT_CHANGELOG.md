# 07_IMPROVEMENT_CHANGELOG — Real Evidence (2026-08-29 v1.3, 7 real +3 synthetic)

| Stage | What you tried and why | Evidence | Decision / Learning |
|-------|------------------------|----------|---------------------|
| Baseline | Single LLM prompt with README + file tree, no tools. Fair basic way per PDF p02. | Baseline 0.418 Spearman (heuristic 85 for is with long README, but actually lint + bus1; 20 for request with short README but actually deprecated) | Kept as baseline — established starting point. |
| Iteration 1 | Added sandbox Build/DepSec tools (real `npm ci`/`audit`/`git log`) to address hallucinated scores — per PDF p02 "better tools". | Hello-World 70 HOLD (evidence/test.log:1), vuln_pass 69 HOLD with 1 CVE (evidence/npm_audit.json:1). Before: 57/57 undifferentiated. After: correctly differentiated. | **Kept — biggest gain**. Proved tool use beats prompt. |
| Iteration 2 | Added verification (drop claims without evidence, discrepancy check) after observing FAIL hallucination on lying README case (PDF p02 verification). | Lying README: baseline 40 (would have scored 80 via README), advanced 55 HOLD + flagged `Discrepancy` with evidence `docker.log:1` + `test.log:1`. Factual accuracy 58%→94%. | Kept — fixed false positive on challenging case, rephrased to "discrepancies between claims and observed behavior". |
| Iteration 3 | Changed orchestration from parallel Build+DepSec → sequential Build→DepSec→Static (fixed race where audit ran before install) + Fixer loop (1 fix + verifier in FRESH sandbox) to improve engineering quality (PDF p02 orchestration). | Vuln_pass 69→81 boost verified: `evidence/patch.diff` JSON_EDIT lodash 4.17.19→4.17.21, `after.log` PASS. Before: fix verification 0%. After: 100% for dep_bump case. | Kept, but limited to 1 fix (tried 3 fixes, removed — time/quality). |
| Iteration 4 | Added 2 real edge cases (moment 69 circular, request 67 deprecated) + 50k LOC flask validation (67 HOLD) to prove real-world messy handling, plus docker 30 + lint 65 penalties. | 10-repo eval: Baseline 0.418 → Advanced 0.915 Delta +0.497. 7 real (is 73, click 78, moment 69, request 67) +3 synthetic (vuln 55, lying 55). Before: synthetic only 0.891 vs 0.709. After: real edge widens gap. | Kept — proves not just synthetic. |
| Removed Experiment | Tried full Next.js dashboard for report viewer — taught us CLI markdown is sufficient for 48h and higher quality (End-to-End 20pts: signed memo quality). | Build time +6h, no metric gain, would have reduced coverage. | **Removed**, saved for post-hackathon. |
| Final | Combined kept changes | **0.418→0.915 Delta +0.497**, fix 69→81 verified, 7 real+3 synthetic transparent `datasets/README.md:1`. | **Main contribution: sandbox reproduction + verification + real edge handling.** Every score → `evidence/<file>:line`, verifier drops hallucinations. |

**Main failure mode (honest):** Windows `shutil.rmtree` PermissionError on git pack files (solved with `onerror` chmod) + single-line JSON BOM patch fragility (solved with JSON_EDIT fallback). LLM still over-scores well-documented bad repos — requires human reviewer for final valuation (ground rule 05).

**Hot Take / Insight (5pts):** Verification (dropping scores without evidence) improved factual accuracy more than adding more tools. For agentic workflows, *subtracting hallucinations* beats *adding capabilities*. Also: Build must precede audit — parallel orchestration caused race (CVE missed), sequential fixed it. Micro1's line "convincing is not enough" is literal — only FRESH sandbox re-run proves correctness.

**Evidence links:** `evaluation_results/metrics.md:1` (0.418→0.915), `out_big/bb69cc12b4b0/report.md:1` (flask 67), `evaluation_results/advanced_runs/049cf4430417/report.md` (69→81), `evaluation_results/advanced_runs/7cdff8a543c0/report.md` (circular 69), `out_real_edge2/4f5188f5f9d9/report.md:1` (moment 69).
